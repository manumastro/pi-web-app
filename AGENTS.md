# Pi Web — Project Guide
Use BLUEPRINT.md as the source of truth for scope, architecture, and phase priorities.
Keep changes modular, type-safe, config-driven, and within file-size budgets.
Prefer explicit dependency injection; avoid globals, magic refs, and dead code.
Backend: Express + TypeScript + SSE/REST with sessions, models, api, and sse layers.
Frontend: React + Vite with URL-driven state and reusable components/hooks.
Add or update tests for every meaningful change; keep builds green.
Use npm workspaces; verify with build/test before handoff.
After each significant change, update BLUEPRINT.md (status snapshot, feature matrix, phase checklists) and this file's current state line.
Current state (2026-04-20): OpenChamber-migrated UI complete with the new chat renderer - light theme with warm/beige palette (oklch-based), IBM Plex Sans/Mono fonts, English labels throughout, 304px sidebar with OpenChamber-style `~`-rooted project/session management (lazy add-project file-tree explorer, searchable sessions, per-project switching, compact session density toggle, project/session dropdown menus, inline session rename, project removal, no model filter), 56px header with session name/project label, composer with Build chip + OpenChamber-style model picker (search, favorites, recent models, collapsible provider groups, full CLI-scoped registry), Tailwind CSS v4 + Radix UI primitives, Zustand stores; new chat UI components: FadeInOnReveal animations, MessageHeader/MessageBody renderers, MarkdownRenderer with syntax highlighting, ScrollToBottomButton, and the new ConversationPanel that groups turns with collapsed reasoning/tool blocks, ordered assistant/tool entries, and wipe reveal animations on mount; legacy ChatMessage/TurnItem/ToolBlock/ThinkingBlock and the permission/question interaction components were removed; backend model selection reuses the shared Pi auth store (`~/.pi/agent/auth.json` + env) so CLI credentials are not duplicated, compaction hooks are disabled to avoid missing-key/totalTokens crashes, systemd launches Bash interactively so `OPENCODE_API_KEY` from `~/.bashrc` is visible to the backend, `/api/config` and `/api/directories` now expose the home directory/file-tree roots for `~` path resolution, and the Settings dialog defaults `show reasoning traces` to on; thinking shown immediately on send with collapsed summary/chevron and cleaned reasoning text, without injecting a literal `thinking…` placeholder; tool calls/results carry `messageId`, backend persists them with stringified inputs/outputs; session status now canonicalizes to OpenChamber-style busy/idle while active sessions remain marked running; turn stack preserves arrival order for interleaved thinking/tool events; historical tool input formatting in `frontend/src/sync/conversation.ts` was consolidated into one helper; frontend re-syncs active model on send and now rehydrates the selected session's running state from persisted status through the new OpenChamber-style `frontend/src/sync/*` layer (`bootstrap`, `child-store`, `event-reducer`, `global-sync-store`, `index`, `input-store`, `live-aggregate`, `notification-store`, `optimistic`, `persist-cache`, `selection-store`, `session-actions`, `session-cache`, `session-prefetch-cache`, `sync-context`, `sync-refs`, `sessionActivity`, `use-app-controller`, `use-sync`, `viewport-store`, `voice-store`) so returning to a live session keeps the running UI visible after reload/tab/session switches and the session CRUD/model/prompt flows are delegated into the sync layer; prompt-send failures now trigger a session reload reconciliation so the view snaps back to the persisted backend state instead of staying on a stale optimistic error view; session list/status now live in `sessionStore.ts` while selection/current-session state lives in `sessionUiStore.ts`; the working indicator now appears as a message-level OpenChamber-style placeholder while the bottom bar stays minimal for abort/error, and the top connection banner is error-only; all 43 frontend tests and 78 backend tests passing, build green, service active on 0.0.0.0:3210.
