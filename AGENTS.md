# Pi Web — Project Guide
Use BLUEPRINT.md as the source of truth for scope, architecture, and phase priorities.
Keep changes modular, type-safe, config-driven, and within file-size budgets.
Prefer explicit dependency injection; avoid globals, magic refs, and dead code.
Backend: Express + TypeScript + SSE/REST with sessions, models, api, and sse layers.
Frontend: React + Vite with URL-driven state and reusable components/hooks.
Add or update tests for every meaningful change; keep builds green.
Use npm workspaces; verify with build/test before handoff.
After each significant change, **always run `npm run build`** and restart the systemd service (`systemctl --user restart pi-web`) when the change affects the production build (CSS/UI changes, API changes, new dependencies, etc.).
After each significant change, update BOTH current-state references: `BLUEPRINT.md` (section **15.0 Status Snapshot**, plus feature matrix/checklists when impacted) and `AGENTS.md` (`Current state` line in this file), keeping the `Current state` entry strictly on a single line consistent with the BLUEPRINT snapshot.
Current state (2026-05-01): Per `BLUEPRINT.md`, Pi Web remains a thin web wrapper over the Pi CLI/RPC runtime; latest source updates make session listing lightweight with summary responses, fix the official `RpcClient` loader path, fix `clearFrontendCacheStorage` so it no longer wipes user preferences (model favorites, recent models, etc.) on every reload, restore inline working placeholders in the active assistant turn until first text chunk (so first-send no longer shows detached working cards), preserve turn-grouping in `buildRenderRecords` when SSE messageIds differ from optimistic turnIds, replace the minimal context chip with a persistent CLI-like status line inside composer actions near the input (↑/↓/R/$ plus context %/window and auto/manual flag), enrich backend status metadata with `cost` and `autoCompactionEnabled` by combining `getSessionStats()` and `getState()`, preserve live `session_status.metadata` across directory snapshot rehydration (fixing the “appears briefly then disappears” context-line bug), prevent first-turn send flashing by avoiding optimistic `streaming` → `idle` downgrade during SSE `onConnected` handshake, add robust fallback logic in `updateLastAssistant` and `applySsePayload` so that `done` / `text_chunk` events with mismatched messageIds still find and update the correct streaming assistant, prevent premature done from propagating through fallback paths, and make reload-resume use the last user id as a stable turn fallback so a running assistant doesn’t spawn a duplicate block after refresh; also fix session ordering to match CLI by sorting with `createdAt` in both backend store and frontend store; load and save sessions using Pi CLI format (subdirectories organized by cwd with timestamp naming, recursive scan of root + subdirectories) so the web app shows exactly the same sessions as `pi ls` (283 vs 104); Pi CLI session files are parsed into web format automatically; new sessions created from the web app are saved to the CLI-style subdirectory with automatic migration (old root file removed on write); expose full context telemetry on mobile too by rendering the CLI-like status line inline in the compact composer plus detailed context/token/cache data in the mobile controls sheet; keep the inline working placeholder focused on runtime state (tooling/permission/retry/writing) instead of thinking text previews; and auto-refresh reasoning timeline summaries from the latest `**...**` segment received in streaming thinking chunks.
